
import { Boot } from 'Boot';
import newWin;


function LoginCheck(input) {

    if (input == "abcd") {
        var ident = newWin.document.getElementById('index.html');
        ident.href = 'main.html';
    }
}